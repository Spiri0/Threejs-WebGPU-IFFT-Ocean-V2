import {THREE} from '../three-defs.js';


export const ocean_constants = (() => {
  	return {
		QT_OCEAN_MIN_LOD_RADIUS: 15,
		QT_OCEAN_MIN_NUM_LAYERS: 15,	
		QT_OCEAN_MIN_CELL_RESOLUTION: 12,	//even numbers only
		OCEAN_SIZE: 500000,
  	}
	
})();
